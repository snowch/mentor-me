<?xml version="1.0" encoding="utf-8"?>
<!--
  App Actions for Google Assistant integration.

  Allows users to create todos via voice commands like:
  - "Hey Google, add a todo on MentorMe: buy groceries"
  - "Hey Google, create a task in MentorMe"
  - "Hey Google, add buy milk to MentorMe"

  Documentation: https://developer.android.com/guide/app-actions/overview
-->
<shortcuts xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- Capability: Create a new todo/task -->
    <capability android:name="actions.intent.CREATE_TASK">
        <!-- Intent that handles the action -->
        <intent
            android:action="android.intent.action.VIEW"
            android:targetPackage="com.example.ai_mentor_coach"
            android:targetClass="com.example.ai_mentor_coach.MainActivity">

            <!-- Pass the task name from voice input -->
            <parameter
                android:name="task.name"
                android:key="todo_title" />

            <!-- Optional: due date extraction -->
            <parameter
                android:name="task.dueDate"
                android:key="todo_due_date" />
        </intent>
    </capability>

    <!-- Capability: Open the app to view todos -->
    <capability android:name="actions.intent.OPEN_APP_FEATURE">
        <intent
            android:action="android.intent.action.VIEW"
            android:targetPackage="com.example.ai_mentor_coach"
            android:targetClass="com.example.ai_mentor_coach.MainActivity">

            <parameter
                android:name="feature"
                android:key="feature_name" />
        </intent>

        <!-- Inline inventory for supported features -->
        <slice-as-shortcut>
            <shortcut android:shortcutId="view_todos">
                <capability-binding android:key="feature">
                    <parameter-binding android:value="todos" />
                    <parameter-binding android:value="tasks" />
                    <parameter-binding android:value="actions" />
                </capability-binding>
            </shortcut>
        </slice-as-shortcut>
    </capability>

    <!-- Static shortcut for quick access - Add Todo -->
    <shortcut
        android:shortcutId="add_todo"
        android:shortcutShortLabel="@string/shortcut_add_todo"
        android:shortcutLongLabel="@string/shortcut_add_todo_long"
        android:icon="@drawable/ic_add_task">
        <intent
            android:action="android.intent.action.VIEW"
            android:targetPackage="com.example.ai_mentor_coach"
            android:targetClass="com.example.ai_mentor_coach.MainActivity">
            <extra android:name="action" android:value="add_todo" />
        </intent>

        <!-- Link this shortcut to the CREATE_TASK capability -->
        <capability-binding android:key="actions.intent.CREATE_TASK" />
    </shortcut>

    <!-- Static shortcut for quick access - Log Food -->
    <shortcut
        android:shortcutId="log_food"
        android:shortcutShortLabel="@string/shortcut_log_food"
        android:shortcutLongLabel="@string/shortcut_log_food_long"
        android:icon="@drawable/ic_food_log">
        <intent
            android:action="android.intent.action.VIEW"
            android:targetPackage="com.example.ai_mentor_coach"
            android:targetClass="com.example.ai_mentor_coach.MainActivity">
            <extra android:name="action" android:value="log_food" />
        </intent>
    </shortcut>

    <!-- Static shortcut for quick access - Log Exercise -->
    <shortcut
        android:shortcutId="log_exercise"
        android:shortcutShortLabel="@string/shortcut_log_exercise"
        android:shortcutLongLabel="@string/shortcut_log_exercise_long"
        android:icon="@drawable/ic_exercise">
        <intent
            android:action="android.intent.action.VIEW"
            android:targetPackage="com.example.ai_mentor_coach"
            android:targetClass="com.example.ai_mentor_coach.MainActivity">
            <extra android:name="action" android:value="log_exercise" />
        </intent>
    </shortcut>

    <!-- Static shortcut for quick access - Start Workout -->
    <shortcut
        android:shortcutId="start_workout"
        android:shortcutShortLabel="@string/shortcut_start_workout"
        android:shortcutLongLabel="@string/shortcut_start_workout_long"
        android:icon="@drawable/ic_workout">
        <intent
            android:action="android.intent.action.VIEW"
            android:targetPackage="com.example.ai_mentor_coach"
            android:targetClass="com.example.ai_mentor_coach.MainActivity">
            <extra android:name="action" android:value="start_workout" />
        </intent>
    </shortcut>

    <!-- Static shortcut for quick access - Reflect (Journal) -->
    <shortcut
        android:shortcutId="reflect"
        android:shortcutShortLabel="@string/shortcut_reflect"
        android:shortcutLongLabel="@string/shortcut_reflect_long"
        android:icon="@drawable/ic_reflect">
        <intent
            android:action="android.intent.action.VIEW"
            android:targetPackage="com.example.ai_mentor_coach"
            android:targetClass="com.example.ai_mentor_coach.MainActivity">
            <extra android:name="action" android:value="reflect" />
        </intent>
    </shortcut>

    <!-- Static shortcut for quick access - Chat with Mentor -->
    <shortcut
        android:shortcutId="chat_mentor"
        android:shortcutShortLabel="@string/shortcut_chat_mentor"
        android:shortcutLongLabel="@string/shortcut_chat_mentor_long"
        android:icon="@drawable/ic_chat">
        <intent
            android:action="android.intent.action.VIEW"
            android:targetPackage="com.example.ai_mentor_coach"
            android:targetClass="com.example.ai_mentor_coach.MainActivity">
            <extra android:name="action" android:value="chat_mentor" />
        </intent>
    </shortcut>

</shortcuts>
